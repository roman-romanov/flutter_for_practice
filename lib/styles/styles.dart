import 'package:flutter/material.dart';

const mainAxisSizeMin = MainAxisSize.min;
const mainAxisSizeMax = MainAxisSize.max;

const edgeInsetsThree = EdgeInsets.all(3.0);
const edgeInsetsFive = EdgeInsets.all(5.0);
const edgeInsetsEight = EdgeInsets.all(8.0);
const edgeInsetsTen = EdgeInsets.all(10.0);
const edgeInsetsTwenty = EdgeInsets.all(20.0);
const edgeInsetsFourty = EdgeInsets.all(40.0);

const edgeInsetsSymHorTen = EdgeInsets.symmetric(horizontal: 10.0);
const edgeInsetsSymHorTwenty = EdgeInsets.symmetric(horizontal: 20.0);
const edgeInsetsSymHorThirty = EdgeInsets.symmetric(horizontal: 30.0);
const edgeInsetsSymHorFourty = EdgeInsets.symmetric(horizontal: 40.0);

const edgeInsetsSymVertTen = EdgeInsets.symmetric(vertical: 10.0);
const edgeInsetsSymVertTwenty = EdgeInsets.symmetric(vertical: 20.0);
const edgeInsetsSymVertThirty = EdgeInsets.symmetric(vertical: 30.0);
const edgeInsetsSymVertFourty = EdgeInsets.symmetric(vertical: 40.0);

const mainAxisAlignmentCenter = MainAxisAlignment.center;
const crossAxisAlignmentCenter = CrossAxisAlignment.center;
const mainAxisAlignmentLeft = MainAxisAlignment.start;
const crossAxisAlignmentLeft = CrossAxisAlignment.start;

final styleSimple_FS10 = TextStyle(fontSize: 10);
final styleSimple_FS20 = TextStyle(fontSize: 20);
final styleSimple_FS25 = TextStyle(fontSize: 25);
final styleSimple_FS30 = TextStyle(fontSize: 30);
final styleSimple_FS40 = TextStyle(fontSize: 40);
final styleSimple_FS50 = TextStyle(fontSize: 50);
final styleDecrement =
    ButtonStyle(backgroundColor: MaterialStateProperty.all(Colors.red));
final iconMenuOpen = Icon(Icons.menu_open);
final iconArrowBack = Icon(Icons.arrow_back, color: Colors.black);

final styleTextButtonFS_20 = TextButton.styleFrom(textStyle: styleSimple_FS20);
final fontFamTNR = TextStyle(fontSize: 20, fontFamily: 'Times New Roman');
const hintStyle = const TextStyle(
  fontSize: 20,
  color: Colors.black26,
  fontFamily: 'Times New Roman',
);
